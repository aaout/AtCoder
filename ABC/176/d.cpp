#include <bits/stdc++.h>
using namespace std;
using ll = long long;
ll H, W;
ll C[2], D[2];
char S[1010][1010];

int main()
{
    cin >> H >> W;
    cin >> C[0] >> C[1];
    cin >> D[0] >> D[1];
    for (int i = 0; i < H; i++)
    {
        for (int j = 0; j < W; j++)
        {
            cin >> S[i][j];
        }
    }

}
